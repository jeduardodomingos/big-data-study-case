applicationName:

spark:
  streamInterval:
  master:

cassandra:
  host:
  port:
  keyspace:

aws:
  region:
  accessKey:
  secretKey:
  sqs:
    consumeQueue:
      address:
      maxMessages:
    produceQueue:
      address:
      maxMessages: